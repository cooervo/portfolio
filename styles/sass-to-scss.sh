# ruby -v
# If for some reason the number is lower than 2.0.0, type sudo gem install ruby to update.

# Now install Sass:
# sudo gem install sass

# Now go to scss dir:
cd scss

# Watch scss/index.scss changes and compile them to ../index.scss:
sass --watch index.scss:../index.css